@model List<StudioCG.Web.Models.ScadenzaDocumentoViewModel>
@using StudioCG.Web.Models
@{
    ViewData["Title"] = "Scadenze Documenti Identità";
    var oggi = ViewBag.Oggi as DateTime? ?? DateTime.Today;
    var dataAvviso = ViewBag.DataAvviso as DateTime? ?? DateTime.Today.AddDays(30);
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h4 class="mb-1"><i class="fas fa-id-card me-2"></i>Scadenze Documenti Identità</h4>
        <p class="text-muted mb-0">Gestione scadenze documenti per tutti i soggetti e clienti (PF, DI, PROF)</p>
    </div>
    <div>
        <a asp-action="Index" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-left me-1"></i>Torna ai Clienti
        </a>
    </div>
</div>

<!-- Statistiche Riepilogo -->
<div class="row mb-4">
    <div class="col-md-2">
        <div class="card bg-danger text-white">
            <div class="card-body text-center py-2">
                <h3 class="mb-0">@ViewBag.TotaleScaduti</h3>
                <small>Scaduti</small>
            </div>
        </div>
    </div>
    <div class="col-md-2">
        <div class="card bg-warning text-dark">
            <div class="card-body text-center py-2">
                <h3 class="mb-0">@ViewBag.TotaleInScadenza</h3>
                <small>In Scadenza (@ViewBag.GiorniAvviso gg)</small>
            </div>
        </div>
    </div>
    <div class="col-md-2">
        <div class="card bg-success text-white">
            <div class="card-body text-center py-2">
                <h3 class="mb-0">@ViewBag.TotaleValidi</h3>
                <small>Validi</small>
            </div>
        </div>
    </div>
    <div class="col-md-2">
        <div class="card bg-secondary text-white">
            <div class="card-body text-center py-2">
                <h3 class="mb-0">@ViewBag.TotaleSenzaScadenza</h3>
                <small>Senza Scadenza</small>
            </div>
        </div>
    </div>
    <div class="col-md-2">
        <div class="card bg-info text-white">
            <div class="card-body text-center py-2">
                <h3 class="mb-0">@ViewBag.TotaleConDocumento</h3>
                <small>Con Documento</small>
            </div>
        </div>
    </div>
    <div class="col-md-2">
        <div class="card bg-primary text-white">
            <div class="card-body text-center py-2">
                <h3 class="mb-0">@ViewBag.TotaleSoggetti</h3>
                <small>Totale</small>
            </div>
        </div>
    </div>
</div>

<!-- Filtri -->
<div class="card mb-4">
    <div class="card-header bg-light">
        <h6 class="mb-0"><i class="fas fa-filter me-2"></i>Filtri</h6>
    </div>
    <div class="card-body">
        <form method="get" asp-action="ScadenzeDocumenti">
            <div class="row g-3">
                <div class="col-md-2">
                    <label class="form-label small">Nome/Cognome/CF</label>
                    <input type="text" name="searchNome" class="form-control form-control-sm" 
                           value="@ViewBag.SearchNome" placeholder="Cerca...">
                </div>
                <div class="col-md-2">
                    <label class="form-label small">Cliente</label>
                    <input type="text" name="searchCliente" class="form-control form-control-sm" 
                           value="@ViewBag.SearchCliente" placeholder="Ragione Sociale...">
                </div>
                <div class="col-md-2">
                    <label class="form-label small">Tipo Soggetto</label>
                    <select name="tipoFiltro" class="form-select form-select-sm">
                        <option value="">-- Tutti --</option>
                        <optgroup label="Soggetti">
                            <option value="LegaleRappresentante" selected="@(ViewBag.TipoFiltro == "LegaleRappresentante")">Legale Rappresentante</option>
                            <option value="Consigliere" selected="@(ViewBag.TipoFiltro == "Consigliere")">Consigliere</option>
                            <option value="Socio" selected="@(ViewBag.TipoFiltro == "Socio")">Socio</option>
                        </optgroup>
                        <optgroup label="Clienti">
                            <option value="PF" selected="@(ViewBag.TipoFiltro == "PF")">PF - Persona Fisica</option>
                            <option value="DI" selected="@(ViewBag.TipoFiltro == "DI")">DI - Ditta Individuale</option>
                            <option value="PROF" selected="@(ViewBag.TipoFiltro == "PROF")">PROF - Professionista</option>
                        </optgroup>
                    </select>
                </div>
                <div class="col-md-2">
                    <label class="form-label small">Stato Scadenza</label>
                    <select name="statoScadenza" class="form-select form-select-sm">
                        <option value="">-- Tutti --</option>
                        <option value="scaduti" selected="@(ViewBag.StatoScadenza == "scaduti")">🔴 Scaduti</option>
                        <option value="inScadenza" selected="@(ViewBag.StatoScadenza == "inScadenza")">🟠 In Scadenza</option>
                        <option value="validi" selected="@(ViewBag.StatoScadenza == "validi")">🟢 Validi</option>
                        <option value="senzaScadenza" selected="@(ViewBag.StatoScadenza == "senzaScadenza")">⚪ Senza Scadenza</option>
                        <option value="conDocumento" selected="@(ViewBag.StatoScadenza == "conDocumento")">📄 Con Documento</option>
                    </select>
                </div>
                <div class="col-md-1">
                    <label class="form-label small">Gg Avviso</label>
                    <select name="giorniAvviso" class="form-select form-select-sm">
                        <option value="7" selected="@(ViewBag.GiorniAvviso == 7)">7</option>
                        <option value="15" selected="@(ViewBag.GiorniAvviso == 15)">15</option>
                        <option value="30" selected="@(ViewBag.GiorniAvviso == 30)">30</option>
                        <option value="60" selected="@(ViewBag.GiorniAvviso == 60)">60</option>
                        <option value="90" selected="@(ViewBag.GiorniAvviso == 90)">90</option>
                    </select>
                </div>
                <div class="col-md-1">
                    <label class="form-label small">Ordina</label>
                    <select name="ordinamento" class="form-select form-select-sm">
                        <option value="alfabetico" selected="@(ViewBag.Ordinamento == "alfabetico")">A-Z</option>
                        <option value="scadenza" selected="@(ViewBag.Ordinamento == "scadenza")">Scadenza</option>
                    </select>
                </div>
                <div class="col-md-2 d-flex align-items-end">
                    <button type="submit" class="btn btn-primary btn-sm me-2">
                        <i class="fas fa-search me-1"></i>Filtra
                    </button>
                    <a asp-action="ScadenzeDocumenti" class="btn btn-outline-secondary btn-sm">
                        <i class="fas fa-times"></i>
                    </a>
                </div>
            </div>
        </form>
    </div>
</div>

<!-- Tabella Risultati -->
<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h6 class="mb-0"><i class="fas fa-list me-2"></i>Elenco Soggetti (@Model.Count risultati)</h6>
        <a asp-action="ExportScadenzeExcel" 
           asp-route-searchNome="@ViewBag.SearchNome"
           asp-route-searchCliente="@ViewBag.SearchCliente"
           asp-route-tipoFiltro="@ViewBag.TipoFiltro"
           asp-route-statoScadenza="@ViewBag.StatoScadenza"
           asp-route-giorniAvviso="@ViewBag.GiorniAvviso"
           asp-route-ordinamento="@ViewBag.Ordinamento"
           class="btn btn-success btn-sm">
            <i class="fas fa-file-excel me-1"></i>Esporta Excel
        </a>
    </div>
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover table-striped mb-0">
                <thead class="table-dark">
                    <tr>
                        <th style="width: 50px;">Stato</th>
                        <th>Cliente</th>
                        <th style="width: 120px;">Tipo</th>
                        <th>Cognome</th>
                        <th>Nome</th>
                        <th>Codice Fiscale</th>
                        <th>N° Documento</th>
                        <th>Data Rilascio</th>
                        <th>Rilasciato Da</th>
                        <th>Scadenza</th>
                        <th style="width: 80px;">Azioni</th>
                    </tr>
                </thead>
                <tbody>
                    @if (!Model.Any())
                    {
                        <tr>
                            <td colspan="11" class="text-center text-muted py-4">
                                <i class="fas fa-search fa-2x mb-2"></i><br>
                                Nessun risultato trovato
                            </td>
                        </tr>
                    }
                    @foreach (var item in Model)
                    {
                        var statoClass = "";
                        var statoBadge = "";
                        var statoIcon = "";
                        
                        if (item.DocumentoScadenza.HasValue)
                        {
                            if (item.DocumentoScadenza.Value < oggi)
                            {
                                statoClass = "table-danger";
                                statoBadge = "bg-danger";
                                statoIcon = "fas fa-exclamation-circle";
                            }
                            else if (item.DocumentoScadenza.Value <= dataAvviso)
                            {
                                statoClass = "table-warning";
                                statoBadge = "bg-warning text-dark";
                                statoIcon = "fas fa-exclamation-triangle";
                            }
                            else
                            {
                                statoClass = "";
                                statoBadge = "bg-success";
                                statoIcon = "fas fa-check-circle";
                            }
                        }
                        else
                        {
                            statoClass = "";
                            statoBadge = "bg-secondary";
                            statoIcon = "fas fa-question-circle";
                        }
                        
                        <tr class="@statoClass">
                            <td class="text-center">
                                <span class="badge @statoBadge">
                                    <i class="@statoIcon"></i>
                                </span>
                            </td>
                            <td>
                                <a asp-action="@(item.IsCliente ? "Edit" : "Details")" asp-route-id="@item.ClienteId" class="text-decoration-none">
                                    @item.ClienteNome
                                </a>
                                @if (item.IsCliente)
                                {
                                    <i class="fas fa-user-circle text-muted ms-1" title="Cliente diretto"></i>
                                }
                            </td>
                            <td>
                                <span class="badge @item.TipoBadgeClass">@item.TipoLabel</span>
                            </td>
                            <td><strong>@item.Cognome</strong></td>
                            <td>@item.Nome</td>
                            <td><code>@item.CodiceFiscale</code></td>
                            <td>@(item.DocumentoNumero ?? "-")</td>
                            <td>@(item.DocumentoDataRilascio?.ToString("dd/MM/yyyy") ?? "-")</td>
                            <td>@(item.DocumentoRilasciatoDa ?? "-")</td>
                            <td>
                                @if (item.DocumentoScadenza.HasValue)
                                {
                                    var giorniRimanenti = (item.DocumentoScadenza.Value - oggi).Days;
                                    <strong class="@(giorniRimanenti < 0 ? "text-danger" : giorniRimanenti <= 30 ? "text-warning" : "text-success")">
                                        @item.DocumentoScadenza.Value.ToString("dd/MM/yyyy")
                                    </strong>
                                    <br>
                                    <small class="@(giorniRimanenti < 0 ? "text-danger" : giorniRimanenti <= 30 ? "text-warning" : "text-muted")">
                                        @if (giorniRimanenti < 0)
                                        {
                                            <text>Scaduto da @Math.Abs(giorniRimanenti) gg</text>
                                        }
                                        else if (giorniRimanenti == 0)
                                        {
                                            <text>Scade oggi!</text>
                                        }
                                        else
                                        {
                                            <text>@giorniRimanenti gg rimanenti</text>
                                        }
                                    </small>
                                }
                                else
                                {
                                    <span class="text-muted">-</span>
                                }
                            </td>
                            <td>
                                @if (item.IsCliente)
                                {
                                    <a asp-action="Edit" asp-route-id="@item.Id" 
                                       class="btn btn-sm btn-outline-primary" title="Modifica Cliente">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                }
                                else
                                {
                                    <a asp-action="EditSoggetto" asp-route-id="@item.Id" 
                                       class="btn btn-sm btn-outline-primary" title="Modifica Soggetto">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<!-- Quick Filters -->
<div class="mt-3">
    <small class="text-muted">Filtri rapidi: </small>
    <a asp-action="ScadenzeDocumenti" asp-route-statoScadenza="scaduti" class="btn btn-sm btn-outline-danger me-1">
        <i class="fas fa-exclamation-circle me-1"></i>Solo Scaduti
    </a>
    <a asp-action="ScadenzeDocumenti" asp-route-statoScadenza="inScadenza" class="btn btn-sm btn-outline-warning me-1">
        <i class="fas fa-exclamation-triangle me-1"></i>In Scadenza
    </a>
    <a asp-action="ScadenzeDocumenti" asp-route-tipoFiltro="LegaleRappresentante" class="btn btn-sm btn-outline-primary me-1">
        <i class="fas fa-user-tie me-1"></i>Legali Rapp.
    </a>
    <a asp-action="ScadenzeDocumenti" asp-route-tipoFiltro="Consigliere" class="btn btn-sm btn-outline-info me-1">
        <i class="fas fa-users me-1"></i>Consiglieri
    </a>
    <a asp-action="ScadenzeDocumenti" asp-route-tipoFiltro="Socio" class="btn btn-sm btn-outline-success me-1">
        <i class="fas fa-handshake me-1"></i>Soci
    </a>
    <a asp-action="ScadenzeDocumenti" asp-route-tipoFiltro="PF" class="btn btn-sm btn-outline-dark me-1">
        <i class="fas fa-user me-1"></i>PF
    </a>
    <a asp-action="ScadenzeDocumenti" asp-route-tipoFiltro="DI" class="btn btn-sm btn-outline-warning me-1">
        <i class="fas fa-store me-1"></i>DI
    </a>
    <a asp-action="ScadenzeDocumenti" asp-route-tipoFiltro="PROF" class="btn btn-sm btn-outline-secondary me-1">
        <i class="fas fa-briefcase me-1"></i>PROF
    </a>
</div>
